name: cargo build

on:
  pull_request:
    branches: [ master, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: cargo build
    - uses: actions/checkout@v2
    - run: cargo build
